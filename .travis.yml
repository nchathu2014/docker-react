sudo: required
language: generic 
services: 
  - docker

before_install:
  - docker build -t nachathu2014/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nachathu2014/docker-react npm run test